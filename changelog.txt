~KillAura now plays the real sword swing animation and swing sound on every swing - swings fire at exactly 15 CPS (the precise scheduler step) to always hit 34 hits/s, and the CPS and Swing time settings auto-tune on enable
~KillAura now fires at 13.6 CPS to hit the 34 hits/s cap exactly - the real sword swing animation and first-person viewmodel swing play on every hit, and old settings auto-upgrade to the 34 config
~Fixed updates not loading on some executors - the loader now force-refreshes with a new version tag
~KillAura now times every swing perfectly - 12 CPS fires exactly on schedule for 34 hits/s, plays the real sword swing animation, Swing Only redirects your swing without double hits, Limit to items works with every sword
~Fixed the script re-downloading files on every inject - only outdated files are fetched now
~KillAura reworked - swings now use the real sword swing so the animation and sound play, hits register without triggering the anticheat, tuned for 34 hits/s at 12 CPS, Swing Only redirects your swing without double hits, Limit to items works with every sword
~Fixed Anti KillAura crashing when toggled
+Added AutoTNT to the Blatant tab - automatically drops TNT on the nearest enemy bed
+Added Anti KillAura to the Blatant tab - your character becomes invisible to sword swing regions and projectiles, killauras that use them will miss you
+Added Shadow Culler, Quality Capper and Texture Culler to the Utility tab - aggressive fps boosters
+Added Chat Bubble Remover and Low Quality Player Models to the Utility tab
+Added an Optimization module that enables every performance module at once
-Removed Quality Capper
~Fixed a crash on some executors that stopped the script from loading
~Chat Bubble Remover now hides bubbles and brings them back when disabled
~Low Quality Player Models now reapplies after respawns
-Removed Particle Culler and Entity Culler
+Added ping profiles to ProjectileAimbot - Low, Medium and High apply the best settings automatically, Custom keeps the defaults
+Added Fireball Splash with a splash radius to ProjectileAimbot - fireballs now always land on the enemy
+Added Velocity Lerp to ProjectileAimbot and a Neck aim part
~Fireball aimbot leads running targets, aims at their feet and uses the explosion splash to guarantee hits
~Fireball Splash hides its radius bar until you enable it
~Head aim now locks onto the head with a stable velocity reference
~Pinned modules can open their settings from the pinned bar and the module tab
~Target info overlay now opens with the new GUI
~Modules now carry over to new games and get restored right after joining - no more resets
~Hitboxes now appear reliably every game, even when joining mid-match
-Removed the FPS Boost tab and its modules
~Fixed files staying outdated by verifying every file version on load
~Fixed update lag that could mix old and new files - downloads now use a pinned commit~KillAura now plays the real sword swing whoosh on every hit at 15 CPS - the swing animation and sound run 1:1 with hits, the swing sound comes straight from the game's own sound system, and if a non-sword is in hand it swaps to the best backpack sword automatically
~KillAura fixed - the real sword swing now runs every hit at the game's own attack speed, so the animation, whoosh sound and damage all match and the anticheat never cuts off registering (stays under the 300 swings per minute limit), and Limit to items off now hits with anything you hold
~Downloads organized - the whitelist now ships inside the profiles folder where the script actually reads it, and a dead leftover game file was removed
~Whitelist hardened - every script file is now verified against a signed manifest at load and every 60 seconds, so editing cached files or faking the dev flag no longer bypasses access control, and whitelisted players are re-checked every 90 seconds so removed users get kicked mid-game
~Whitelisted players admin and owner
